---
apiVersion: terraform.techtales.io/v1alpha1
kind: MinioUser
metadata:
  name: volsync-nas
  namespace: nas.techtales.io
spec:
  description: volsync nas backup account
  disabled: false
  permissions:
    - bucket: volsync
      path: "/nas/*"
      actions:
        - "*"
