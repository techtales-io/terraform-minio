---
apiVersion: terraform.techtales.io/v1alpha1
kind: MinioUser
metadata:
  name: cnpg-kube-lab
  namespace: nas.techtales.io
spec:
  description: cnpg kube-lab backup
  disabled: false
  permissions:
    - bucket: cnpg
      path: "/kube-lab/*"
      actions:
        - "*"
