---
apiVersion: terraform.techtales.io/v1alpha1
kind: MinioUser
metadata:
  name: alex
  namespace: nas.techtales.io
spec:
  description: alex backup user
  disabled: false
  permissions:
    - bucket: backup
      path: "/*"
      actions:
        - "GetObject"
        - "ListBucket"
    - bucket: backup
      path: "/alex/*"
      actions:
        - "*"
