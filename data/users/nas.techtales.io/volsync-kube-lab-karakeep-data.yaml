---
apiVersion: terraform.techtales.io/v1alpha1
kind: MinioUser
metadata:
  name: volsync-kube-lab-karakeep-data
  namespace: nas.techtales.io
spec:
  description: volsync kube-lab backup account
  disabled: false
  permissions:
    - bucket: volsync
      path: /*
      actions:
        - ListBucket
    - bucket: volsync
      path: /kube-lab/productivity/karakeep-data/*
      actions:
        - GetObject
        - PutObject
        - DeleteObject
