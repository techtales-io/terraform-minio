---
apiVersion: terraform.techtales.io/v1alpha1
kind: MinioUser
metadata:
  name: volsync-kube-lab-apprise-config
  namespace: nas.techtales.io
spec:
  description: volsync kube-lab backup account
  disabled: false
  permissions:
    - bucket: backup
      path: /*
      actions:
        - ListBucket
    - bucket: backup
      path: /kube-lab/notification/apprise-config/*
      actions:
        - GetObject
        - PutObject
        - DeleteObject
