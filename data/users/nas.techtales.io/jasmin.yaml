---
apiVersion: terraform.techtales.io/v1alpha1
kind: MinioUser
metadata:
  name: jasmin
  namespace: nas.techtales.io
spec:
  description: jasmin backup user
  disabled: false
  permissions:
    - bucket: backup
      path: "/*"
      actions:
        - "GetObject"
        - "ListBucket"
    - bucket: backup
      path: "/jasmin/*"
      actions:
        - "*"
