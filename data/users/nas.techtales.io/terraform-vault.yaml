---
apiVersion: terraform.techtales.io/v1alpha1
kind: MinioUser
metadata:
  name: terraform-openbao
  namespace: nas.techtales.io
spec:
  description: terraform state backend for terraform-openbao
  disabled: false
  permissions:
    - bucket: terraform
      path: "/*"
      actions:
        - "GetObject"
        - "ListBucket"
    - bucket: terraform
      path: "/openbao/*"
      actions:
        - "*"
