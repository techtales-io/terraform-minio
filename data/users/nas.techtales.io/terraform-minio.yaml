---
apiVersion: terraform.techtales.io/v1alpha1
kind: MinioUser
metadata:
  name: terraform-minio
  namespace: nas.techtales.io
spec:
  description: terraform state backend for terraform-minio
  disabled: false
  permissions:
    - bucket: terraform
      path: "/minio/nas.techtales.io/*"
      actions:
        - "*"
