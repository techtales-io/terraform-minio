---
apiVersion: terraform.techtales.io/v1alpha1
kind: MinioUser
metadata:
  name: volsync
  namespace: tyriis.dev
spec:
  description: volsync backups
  disabled: false
  permissions:
    - bucket: volsync
      path: "/*"
      actions:
        - "*"
