---
apiVersion: terraform.techtales.io/v1alpha1
kind: MinioUser
metadata:
  name: loki
  namespace: tyriis.dev
spec:
  description: loki logging storage
  disabled: false
  permissions:
    - bucket: loki
      path: "/*"
      actions:
        - "*"
